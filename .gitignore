*.iml
.gradle
local.properties
.DS_Store
/build
build/
/captures
.externalNativeBuild
/release
/tmp
node_modules
node_modules/
**/node_modules/
/app/app
/app/google
/app/gradle.properties
.idea/
# Kotlin 2.0
.kotlin/
.cxx
.vscode
# local E2E runtime web bundle (copied from web/dist)
server/src/main/resources/static/
.env
.data/
# Local runtime data directory used on Windows (SQLite WAL + covers + installed plugins)
# Anchored to the repository root on purpose: an unanchored `dist/` would also hide the
# tracked web/dist bundle, which the server embeds into its static resources.
/ls_data/
# Locally assembled runnable artifacts (built fat JAR and its backups)
/dist/
.agents/
.worktrees/
coverage/
web/coverage/

