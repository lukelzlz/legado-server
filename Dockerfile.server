FROM docker.m.daocloud.io/library/node:22-alpine AS web-build
WORKDIR /src/web
COPY web/package.json ./
RUN npm install --ignore-scripts
COPY web/ ./
RUN npm run build

FROM docker.m.daocloud.io/library/gradle:8.13-jdk21 AS server-build
WORKDIR /src
COPY gradle/ gradle/
COPY gradle/libs.versions.toml gradle/libs.versions.toml
COPY server/ server/
COPY --from=web-build /src/web/dist/ server/src/main/resources/static/
RUN --mount=type=cache,target=/home/gradle/.gradle gradle -p server installDist --no-daemon

FROM docker.m.daocloud.io/library/eclipse-temurin:17-jre-jammy
RUN apt-get update && apt-get install -y --no-install-recommends gosu && rm -rf /var/lib/apt/lists/* \
    && groupadd --system legado && useradd --system --gid legado legado
WORKDIR /app
COPY --from=server-build /src/server/build/install/legado-server/ ./
COPY server/docker-entrypoint.sh /app/entrypoint.sh
RUN chmod 755 /app/entrypoint.sh
ENV LEGADO_DATA_DIR=/data LEGADO_PORT=8080 LEGADO_SECURE_COOKIES=true
EXPOSE 8080
ENTRYPOINT ["/app/entrypoint.sh"]
CMD ["/app/bin/legado-server"]
