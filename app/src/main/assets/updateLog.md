# 更新日志

## cronet版本: 151.0.7922.29

**2026/07/22**
- 听书新增可选目录的离线音频缓存，支持范围缓存、目录缓存状态、损坏自动回源，以及取消或失败时保留已有有效缓存
- 新增书源原生段评统计与详情支持，修复章节切换时段评图标串用及空正文误加载的问题
- JavaScript 单文件书源新增原生段评统计与详情函数支持，补充函数配对校验、分页和嵌套回复兼容
- Web API 新增带令牌保护的 HTTP 请求日志只读查询接口，支持摘要列表与单条详情
- 书籍详情页的长文本、HTML 与 Markdown 简介支持折叠和展开
- 新增带令牌与 Host/Origin 防护的原生 MCP 书源开发服务，支持书源保存、调试、查询、删除和脱敏 HTTP 日志工具
- 修复自定义主题下部分浮层菜单与历史下拉框背景未随主题变化的问题
- 修复正式版构建使用浅克隆时版本号计数回退的问题，确保 `versionCode` 按完整提交历史递增
- 恢复旧版 JavaScript 书源的块内 `const` 作用域兼容，修复部分段评页面持续加载的问题

**2026/07/20**
- 修复动态登录表单初始化时脚本反向读取登录信息导致 Rhino 递归溢出的问题
- 恢复 JavaScript 规则中 `java.getElements(...)` 返回结果的 Jsoup 链式调用兼容，支持继续使用 `attr`、`text`、`html` 等旧式写法
- 恢复旧版 JavaScript 源对 `with` 作用域、E4X XML 后代选择和嵌套 `eval` Java 方法调用的兼容
- Web API 新增带令牌保护的纯 JavaScript 单文件书源保存接口，统一保护书源写入、搜索和调试，令牌不再持久化，并加强正文 HTML、接口响应、请求大小、幂等保存和相似 URL 状态隔离
- Brotli 响应改为直接使用底层流式解码器，移除不再需要的 OkHttp 包装依赖并保持 br 解压与资源关闭行为
- 修复 JavaScript 单文件书源解析被取消时仍按普通失败处理的问题，避免离开编辑或导入流程后继续错误收尾
- 兼容 Cronet 151 对 Android 未来版本 Private Compute 服务的条件引用，修复 Release R8 缺失类导致的编译失败
- 修复 Cronet 更新任务解析 ABI 文件名时误读字符串属性，恢复版本下载、校验与元数据生成
- 修复本地 MOBI 与 PDF 打开时关闭旧解析器触发递归重载，导致文件一直加载或无法打开的问题
- 检查本地书籍文件是否存在后及时关闭输入流，避免重复进入阅读页积累文件句柄
- 收紧视频播放器的 R8 保留范围，仅保留运行时反射构造器并清理失效的 Media3 私有字段规则，减少安装包体积
- 精简代码编辑器的 R8 保留规则，交由 TextMate 依赖保留反射模型并移除未使用实现，减少安装包体积
- 精简当前运行时未使用的 Kotlin 内置元数据，减少安装包体积
- 压缩 TextMate 语法与主题配置，在保持 JSON 结构不变的前提下减少安装包体积
- 精简内置帮助页代码高亮资源，仅保留文档使用的 JavaScript、Java、HTML/XML 与纯文本语言，减少安装包体积
- 优化无效书籍缓存清理，普通清理仅查询必要字段，并仅在启用漫画保留策略时读取完整漫画数据
- 优化阅读背景预览加载，直接交由图片缓存读取内置资源，避免列表绑定时同步复制完整图片数据
- 内置 CryptoJS 4.2.0 兼容库，为规则与纯 JS 书源提供 MD5、SHA、HMAC、Base64、AES 等能力，并使用系统安全随机数生成随机盐
- 书源管理列表增加 JS 标识，便于快速区分纯 JavaScript 单文件书源
- 优化规则大数据目录清理，批量校验书籍与 RSS 关联并在删除前复核，避免残留无效空目录
- 升级 JavaScript 执行引擎，兼容 `for (const ... in ...)` 与块级 `const`，修复部分 JS 书源导入或保存时报语法错误的问题
- 优化 RSS 源导入预览的批量匹配，并避免界面重建时重复加载导致状态错位
- 修复 RSS 阅读记录标题缺失时页面无法加载，并兼容对应订阅源已删除的情况
- 优化替换规则导入预览时的数据库查询，批量匹配并保持原导入顺序
- 优化选中书源导出时的数据库查询，批量解析并保持原选择顺序
- 修复单个 RSS 订阅源 JSON 无法重新导入的问题
- 修复换源目录加载与 WebDAV 备份查询吞掉协程取消信号的问题
- 修复在线导入排版配置时再次导入重复添加、空列表未写入或成功提示早于持久化完成的问题
- 修复部分 Content Provider 或特定长度的 JSON 文件识别失败问题

**2026/07/19**
- 收紧网络、Cronet 与权限队列的类型边界，修复请求取消后潜在的缓冲区空引用并减少 Release 编译告警
- 修复部分语言下视频书源类型缺失，并稳定 EPUB 文件类型在不同系统语言下的识别
- 修复书籍导出文件名未统一清洗保留字符，导致普通或分卷导出失败的问题
- 自定义导出文件名脚本返回空值时改用默认名称，并在分卷导出配置中提示规则无效
- 精简安装包中未使用的依赖语言与构建资源，并保留应用现有全部翻译
- WiFi 传书页移除旧版 jQuery 依赖，修复网络失败重复收尾，并保留选择与拖拽上传
- 修复压缩文件解包时相邻同前缀目录可能绕过目标目录边界校验的问题
- 修复外部文件名可越过本地书籍保存目录的问题
- 修复 Content Provider 的 RSS 源保存、查询和删除请求错误路由到书源处理的问题
- 修复 RSS 文章分页失败后无法重试、页码跳过和加载状态无法结束的问题
- 修复 RSS 分类脚本异常或格式损坏时无分类结果并可能导致调试崩溃的问题
- 修复书源校验在目录仅含卷标题时误判为网站失效的问题
- 排除当前未使用的媒体投屏依赖，保留现有播放器能力并减小安装包体积
- 目录分卷默认全部展开，并保留通过卷标题右侧按钮按需折叠的交互
- 恢复书籍详情页普通文本简介的段落首行缩进
- 优化规则编辑器辅助键盘的输入法显示判断，避免全面屏和手势导航下误显示或漏显示
- 修复导入旧版书源时多条发现规则中的空地址被转换为字面量 `null` 的问题
- JavaScript 单文件书源新增 `config` 配置对象，释放运行时 `source` 书源实体绑定，并兼容旧版配置与 `sourceApi` 调用
- JavaScript 单文件书源支持在代码编辑器中直接保存并进入调试，返回后继续编辑
- 修复书源与 RSS 源包含异常组合字符时可能卡死编辑页的问题，并提供安全编辑入口

- 修复动态登录 UI 加载期间取消脚本可能崩溃，并增加加载状态提示
- 修复分组改名时相似名称或含通配符的组被误加入新分组的问题

**2026/07/18**
- 书架管理支持仅更新已选书籍的目录，并跳过本次更新的正文预下载
- 修复空登录表单分流及脚本跨源登录可能打开错误源的问题
- JavaScript 单文件书源调试会同时展示函数原始返回值与解析后的书籍、详情、目录和正文摘要
- JavaScript 单文件书源支持 WebView 登录和表单登录，表单字段与登录函数会在导入时校验
- 支持导入、编辑、调试和导出 JavaScript 单文件书源，并接入搜索、发现、详情、目录与正文读取流程
- 修复 Cronet 预下载完成后状态未复位导致后续重试被跳过的问题，并收紧下载文件与历史目录的资源清理
- 本地备份会同时保存自定义封面和阅读背景图片，恢复时使用暂存目录避免复制失败覆盖现有文件
- EPUB、PDF 和 MOBI 本地书在切换文件或清理解析缓存时会立即释放文件描述符，避免连续阅读积累句柄
- 精简阅读菜单亮度滑杆实现，移除旧版反射控件并保持由下向上的调节方向
- 书架列表关闭频繁数据刷新时的默认条目动画，减少阅读进度、更新时间和更新状态变化造成的闪烁
- 编辑 JavaScript 单文件书源时直接进入代码编辑器，不再短暂显示声明式书源表单
- 修复在书源导入预览中编辑条目后新增、更新状态及默认勾选未同步的问题

**2026/07/17**
- 书源与 RSS 调试、翻页按键及视频控制等圆角按钮统一使用原生波纹反馈，并清理旧式按压背景资源
- HTTP TTS 朗读引擎支持配置段落间隔，下载与流式播放均会在相邻段落之间插入指定时长的静音
- HTTP TTS 朗读引擎支持在登录状态过期后直接重新打开登录页，不再被历史登录信息阻断
- 听书与朗读支持按剩余章节数停止，分钟定时和章节计数互斥且仅在自然播完时扣减
- 修复靠近屏幕底部的操作菜单可能显示到可视区域外的问题，空间不足时自动向上展开
- 听书会在当前章节开始播放后预加载下一章地址，切换章节、书籍或书源时自动校验并失效旧结果
- 书源网页登录背景跟随当前自定义主题色，并继续遮挡透明窗口下层页面
- 书架支持按需显示阅读进度，列表提供百分比与进度条，网格在封面底部显示进度条
- 书籍详情页的书架操作使用强调色浅底样式，并统一横竖屏按钮间距与触控高度
- 阅读页页眉页脚支持组合书名、章节、时间、电量、页码和进度模板，并提供填充式电量图标
- 阅读页页眉、正文和页脚的上下填充范围统一扩展到 400，适配更大的阅读留白需求
- 修复书源网页登录界面短暂透出上一页面控件的问题
- 阅读中手动翻页不再打断正在播放的朗读，并可从朗读面板一键回到实际朗读章节和位置
- 朗读设置新增“手动翻页时跟随朗读”，可选择在翻页后从新页面重新开始朗读
- 朗读位置与当前页面脱离时显示快捷操作条，可直接回到朗读位置或从当前页面重新朗读
- 朗读快捷操作条启用后移除朗读弹窗内重复的回位按钮，避免跟随状态下显示无效入口
- java.showBrowser 支持分别配置底部浏览器的折叠与展开高度，网页上滑时可在限定范围内展开
- java.showBrowser 可从通用书源脚本上下文打开底部浏览器，仅在应用存在前台界面时响应
- 修复设置页眉页脚模板时占位符按钮主题不兼容导致的崩溃
- 优化大目录倒序切换，整表重排时跳过无效差异计算并避免重复刷新
- 阅读正文的文本选择菜单支持自定义浮动条与更多菜单的操作分组和顺序，并兼容旧版展开设置
- 可滚动文本与规则输入框的选区高亮跟随当前主题强调色
- 应用从后台返回主界面时可识别分享口令，并避免内部页面切换或界面暂停期间误触发导入
- 书源 JavaScript 新增 singleFlight、lock 与 tick 并发工具，支持请求合并、互斥执行和轮询计数
- java.htmlFormat 支持传入当前页面地址，自动补全正文图片的相对链接
- 其他设置新增可选 HTTP 请求日志，最近记录仅保留在内存中并自动脱敏认证、Cookie 与密钥字段

**2026/07/16**
- 源、规则与朗读引擎的直链导出支持口令分享，主界面在隐私授权后可安全识别剪贴板口令并校验有效期
- 精简备份恢复设置，将阅读2.0数据导入移至右上角菜单并保留完整迁移能力
- 清理 WiFi 传书页面中误混入的浏览器扩展样式与内嵌字体，减小安装包体积
- 优化书籍详情编辑页在小屏、横屏和键盘弹出时的滚动与输入布局
- 书源、RSS 源及登录表单的多行输入框扩大触控高度与文字内边距
- 简介清洗不再自动添加全角缩进，结构化简介按行显示
- 发现页支持将已加载结果批量加入书架，并安全跳过重复书籍

**2026/07/15**
- 目录支持按分卷折叠，搜索时显示匹配数量并补充分卷上下文
- 书籍详情页标签支持自动换行和逐行居中，搜索与发现列表继续保持单行显示
- 优化超长规则文本编辑，超过高亮阈值时停止重复调度与全文制表符扫描
- 修复代理格式校验、认证重试及 Cronet 绕过代理的问题，兼容旧版和标准 HTTP 认证格式
- URL 请求支持读取超时和重定向开关，固定 DNS 仅作用于当前目标域名，并避免关闭重定向时重复提交请求
- 升级 ZXingLite 至 3.5.0，适配新版解码配置并改善扫码相机停止时的稳定性

**2026/07/14**
- 优化发布构建兼容性与稳定性
- 优化辅助按键编辑弹窗的输入框触控尺寸与字段间距
- 修复部分系统上音频播放与暂停按钮图标不可见
- 提升目录与书签页面重建时的稳定性
- 通过网址添加书籍时会自动加入当前书架分组
- 优化规则编辑器快速输入时的语法高亮性能
- 无简介的书籍与视频详情页不再保留空白占位
- 修复 WebView 复用时书源 User-Agent 串用及内部请求头外发

**2026/07/13**
- 后台 webJs 控制台输出会显示在书源调试日志中
- 修复自定义文件上传未应用超时和 DNS 配置
- 修复部分系统文件选择器拒绝持久授权时崩溃
- 修复搜书与发现列表长信息被封面高度裁切
- 前台服务权限理由在同一进程内不再重复提示
- 修复书内搜索结果的横向布局约束错误
- 朗读面板支持查看并快捷切换当前引擎
- 搜书时显示已完成书源数和总书源数
- TXT 目录规则支持按名称和示例搜索
- 修复搜索框文字与光标在部分主题下看不清
- 图片查看器中的小图会自动适配可用空间
- 恢复小时级版本号并修复应用内更新下载
- 通用 APK 使用稳定标识，仅发布到 GitHub Release 与 CDN
- 修复未缓存章节导出时出现 null 文本
- 应用更新仅保留 GitHub，清理无调用的 Gitee 更新实现
- 应用内更新会按设备架构选择 ARM 小包或通用包
- 自动发布改为使用版本号标签的正式 Release
- 新增 ARM 小体积 APK，并保留带通用标识的全架构安装包
- 修复阅读排版与翻页并发时偶发的页面索引异常
- 修复应用启动早期偶发的 RhinoContext 初始化异常
- HTTP 网络请求支持 Brotli 压缩响应
- 修复 JS 对象继续使用 JSONPath 解析时返回空结果
- JS 网络方法支持使用 JSON 字符串传入请求头
- 修复 RSS 网页登录 Cookie 未同步到后续请求
- RSS 阅读页横竖屏切换改用系统窗口边衬控制，横屏时仍可通过滑动临时显示状态栏
- 修复书源登录页加载时透出上一页面内容
- 修复仅配置登录表单的 HTTP TTS 无法正常登录
- Bump Cronet from 143.0.7499.194 to 150.0.7871.114
- Beta Release 发布说明自动读取最近更新内容
- 未启用 Cronet 时不再预下载内核
- 优化文件导入、上传与图片缓存的 I/O 稳定性
- 优化 WebView 缓存及诊断资源管理
- 修复文件类书源下载文件名重复后缀
- 修复 SVG 图片按目标尺寸放大时模糊
- 修复 HTTP TTS 登录头或源变量变化后错误复用旧音频缓存
- 自定义封面不再向第三方地址附带书源认证信息
- 前台服务启动受系统限制时安全停止，避免崩溃

**2026/07/04**
- 链接规则支持head类型
- 回调事件支持详情页点击标签
- 详情页简介图片支持执行js，支持长按查看
- 优化web端加载段评图片
- 详情页简介支持用web渲染
- 调整新用户预加载和搜索线程的默认值
- 其余优化与修复

**2026/03/07**
- 优化代码，修复问题

**2026/03/03**
- 优化代码，修复问题
- 视频悬浮窗播放时进行系统媒体播放通知
- 净化规则使用js时支持调用java.log
- 代码编辑器搜索替换内容支持$符号
- 优化书架滚动位置记忆
- 增加搜索结果排序时对书籍分类信息进行判断
- 增加自动检查app更新功能

**2026/02/16**
- 优化代码，修复问题
- 让小说朗读走系统媒体播放通道
- 更新内置字典规则
- 新增java.refreshBookToc函数
- java.reLoginView函数增加deltaUp参数
- 新增@webjs:规则类型
- 文件类书源支持下载链接type指定文件后缀
- 提升购买按钮权限

**2026/01/31**
- 优化代码，修复一些问题
- 正文增加锁定反向横屏

**2026/01/28**
- 新增java.reLoginView()函数，刷新登录界面
- 书源发现支持更多丰富的按钮类型
- 新增java.refreshExplore()函数
- java.open函数支持打开登录界面
- 书源简介支持html标签包裹，显示html样式
- 书籍简介和字典支持gif动态图和svg图data链接
- 书籍简介和字典支持button按钮
- 支持源控制图片显示尺寸
- 书籍简介支持markdown语法编写
- 新增java.showBrowser函数，能进行半屏显示段评
- 支持图片链接click键，不推荐继续使用旧方式
- 支持双击响应段评图
- 新增chapter.update()函数
- 新增java.showPhoto函数
- 新增java.refreshContent()函数
- 支持订阅源启动页html用js返回空
- 提升webview函数获取js结果速度
- 其余优化与修复

**2026/01/13**
- 升级cronet版本至：143.0.7499.193
- 软件自定义背景图支持.9.png格式
- 背景图导入支持直接输入图片在线链接
- 主题分享支持在线背景图链接
- 背景图支持跟随主题切换
- 主题设置支持透明操作栏，提升图片背景视觉效果
- 支持分组封面自定义图片恢复默认
- 登录UI的select类型支持action键
- 提升内置浏览器打开速度（例：订阅源、段评 打开速度大概快100毫秒左右）
- 支持正文下划线设为虚线类型
- cache.get函数新增onlyDisk参数
- tts源支持jslib规则
- tts源登录界面新增java.clearTtsCache()函数
- 支持导出单个tts源
- 编辑tts源、字典规则、TXT目录规则时误触空白区域会提示保存
- 新增正文边缘点击阈值设置，防止曲面屏误触
- 实现订阅源的登录检查规则
- 在链接访问出错时，也能执行一次登录检查规则
- StrResponse对象支持callTime()获取响应时间
- 并发访问函数支持skipRateLimit参数，绕过源并发率限制
- 视频播放器支持记录函数调用时的播放进度
- 其余细节优化与bug修复


## **必读**
【温馨提醒】 *更新前一定要做好备份，以免数据丢失！*

* 阅读只是一个转码工具，不提供内容，第一次安装app，需要自己手动导入书源。
* 正文出现缺字漏字、内容缺失、排版错乱等情况，有可能是净化规则或简繁转换出现问题。
----

* [2025年日志](https://github.com/LegadoTeam/legado/blob/record2025/app/src/main/assets/updateLog.md)　
* [2023年日志](https://github.com/LegadoTeam/legado/blob/record2023/app/src/main/assets/updateLog.md)　
* [2022年日志](https://github.com/LegadoTeam/legado/blob/record2022/app/src/main/assets/updateLog.md)　
* [2021年日志](https://github.com/LegadoTeam/legado/blob/record2021/app/src/main/assets/updateLog.md)　
